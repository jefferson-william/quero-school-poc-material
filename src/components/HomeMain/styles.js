import styled from 'styled-components'

export const HomeMain = styled.main`
  section {
    padding: var(--space-extra-jumbo) 0;
  }
  h2 {
    margin: 0 0 var(--space-extra-large) 0;
    text-align: center;
  }
  .home-main {
    &__container {
      flex-direction: column;
      display: flex;
    }
  }
`
